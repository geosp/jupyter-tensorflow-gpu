apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tensorflow-jupyter-data-pv-claim
spec:
  storageClassName: shared-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
